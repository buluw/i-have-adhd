# Codex 防过度设计组合：使用说明

## 分工

| 组件 | 负责什么 | 是否自动 |
| --- | --- | --- |
| `i-have-adhd` | ADHD 友好回复、保持当前动作和状态、抑制支线、避免替用户做设计决定 | 每个新任务手动启用一次 |
| `stop-that-shit` | 防止越权修改、防御性工程、无用依赖、重复验证和任务扩散 | Hook 自动加载；任务模式手动指定 |
| `ponytail-review` | 检查当前 diff 中已经产生的过度设计 | 手动，只报告不修改 |
| `ponytail-audit` | 在历史项目中查找已有复杂度和废弃抽象 | 手动低频使用，只报告不修改 |

## 日常任务

先启用 ADHD 模式：

```text
$i-have-adhd
```

然后直接给任务。存在明显边界或扩散风险时，用：

```text
$stop-that-shit change -- 只完成当前明确需求，复用现有架构，做最小修改和一次定向验证。
```

只允许查看和报告时，用：

```text
$stop-that-shit review -- 检查当前内容，只报告，不编辑任何文件。
```

`stop-that-shit` 的 Hook 会在新任务中加载，但默认只观察；显式使用 `change`、`review`、`answer` 或 `monitor` 后才会按该任务边界工作。

## 清理当前任务产生的过度设计

在已有 diff 上手动调用：

```text
$ponytail-review 检查当前 diff 的过度设计，只报告可以删除或缩短的内容，不修改文件。
```

确认某一项确实要处理后，再单独执行：

```text
$stop-that-shit change -- 只处理 ponytail-review 的第 1 项，不顺带重构其他内容。
```

## 清理历史项目

低频运行审计，并限制候选数量：

```text
$ponytail-audit 审计整个仓库，只返回置信度最高的 3 个候选，不修改文件。
```

人工选择一个候选后再处理：

```text
$stop-that-shit change -- 只处理 ponytail-audit 的第 1 项，保留现有行为并做一次最小验证。
```

不要自动把审计结果全部修改；一次处理一个已确认候选。普通功能任务不要调用 `ponytail-audit`，`simplify-codebase` 不属于这套组合。

## 遇到阻塞

- 能在既有需求和架构内解决：继续执行。
- 需要用户能快速提供的信息：直接询问，不操作浏览器或电脑绕路获取。
- 需要新的设计决定：停止并报告选择点，不替用户决定。
- 用户明确授权的托管任务：可以在授权范围内继续，不扩大授权。

停止 ADHD 模式：

```text
stop adhd mode
```
